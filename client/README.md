# Wally

A client-only, browser-based genome viewer built on the wally command-line application.

## Prerequisites

The WebAssembly of wally is build using

```bash
cd .. && make all && make wasm
```

## Web Development

```bash
cd client 
npm install
npm run dev
```

## Web Build

```bash
npm run build
```
